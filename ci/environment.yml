name: spice-ci
channels:
  - conda-forge
dependencies:
  # Required Phase 1 subset of environment.yml; tree plotting/tools are later.
  - python=3.11
  - pandas=2.2.3
  - r-base=4.3.3
  - r-ape
  - r-coda
  - r-janitor
  - r-posterior=1.6.0
  - r-dplyr
  - r-progress